# Config PUBLIQUE partagée (committée) — AUCUN secret ici.
# Secrets (ARBORE_API_KEY, clés Firebase serveur, SENTRY_AUTH_TOKEN) : via
# l'environnement de déploiement ou un fichier .env*.local (gitignored).
# Template complet : voir .env.example

# Sentry — DSN public (navigateur), identique en local et en prod.
NEXT_PUBLIC_SENTRY_DSN=https://cb706774dbec3ec705a65fa748c35c8c@o4511095262216192.ingest.de.sentry.io/4511526618136656
# Environnement Sentry : laissé vide -> dérive automatiquement de NODE_ENV
# (development en local, production au build prod).
NEXT_PUBLIC_SENTRY_ENV=
